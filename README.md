# INFO-H512-Current-trends-in-artificial-intelligence-202526

## Running the Project

To run the notebook without issues, we recommend using Google Colab.

Before executing the code, please make sure that the dataset `car_insurance_claim.csv` is available in your Google Drive and that the notebook path is configured accordingly.

**Note:** The results obtained may differ slightly from those reported in the notebook and report. During development, we inadvertently omitted setting a random seed (`random.seed` / `np.random.seed`), which affects the reproducibility of some experiments.
